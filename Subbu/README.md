# Subba Rayudu Devalla — Portfolio

Personal portfolio site with an interactive 3D neural network hero.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

Stack: React, Vite, Three.js (`@react-three/fiber` + `drei`), Framer Motion.
